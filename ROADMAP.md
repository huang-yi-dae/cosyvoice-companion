# Roadmap · 优化与演进路线

本文件记录 `cosyvoice-companion` 的可优化方向与演进计划。项目定位为**个人研究用途**：
从 QQ 聊天记录重建某人的声音（CosyVoice 克隆 / 云端 TTS）与人设（companion agent）。

优先级标记：🔴 高（建议尽快）・🟡 中・🟢 低（锦上添花）
状态标记：⬜ 未开始・🚧 进行中・✅ 已完成

---

## 一、工程质量与可维护性

| # | 方向 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | **自动化测试** | 项目自身当前**零测试**。补 `pytest` 冒烟/单元用例：`config` 加载与路径解析、`TTSProvider` 抽象、`clean`/`export_msgs` 纯逻辑、`dashscope_tts` 缺 Key 报错。目标先覆盖不依赖 GPU/网络的纯逻辑。 | 🔴 | ✅ |
| 2 | **CI 流水线** | 加 `.github/workflows/ci.yml`：`ruff` lint + `pytest`（仅跑纯逻辑用例）。注意排除 vendored `internal/src/CosyVoice/`。 | 🔴 | ✅ |
| 3 | **拆分 app.py** | `web/app.py` 已 767 行，混合工作室/云端/流水线/模型/用户/对话逻辑。按 FastAPI `APIRouter` 拆为 `routers/{studio,cloud,pipeline,models,users}.py`，`app.py` 只做装配。 | 🟡 | ⬜ |
| 4 | **结构化日志** | 现多处用 `print()`。引入 `logging`（分级、可写文件），Web 端把模型加载/合成耗时、失败原因统一记录，便于排查首次加载慢等问题。 | 🟡 | ⬜ |
| 5 | **配置校验** | 用 `pydantic-settings` 对 `.env` / `config.yaml` 做启动期校验，缺失关键项时给出明确报错而非运行时崩溃。 | 🟢 | ⬜ |
| 6 | **前端 JS 抽离** | `index.html` 等为内联 JS 单文件（~500 行）。可抽到 `static/*.js` 模块，减少重复（多页共用 `/api/config`、用户切换逻辑）。 | 🟢 | ⬜ |
| 7 | **瘦身仓库** | vendored `internal/src/CosyVoice/` 体积庞大且含其自身 CI/示例。考虑改为 git submodule 或按需下载，降低克隆成本。 | 🟢 | ⬜ |

---

## 二、安全与隐私（隐私敏感项目重点）

| # | 方向 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 8 | **Web 访问鉴权** | 当前 Web 无任何认证。`/pipeline` 可触发**解密**流程，一旦 `web.host=0.0.0.0` 暴露到局域网即有风险。加最简 token/basic-auth，或强约束仅 `127.0.0.1` 监听。 | 🔴 | ✅ |
| 9 | **敏感操作确认与审计** | 密钥提取、解密等高危步骤增加二次确认与操作日志（谁、何时、对哪个 QQ）。 | 🟡 | ⬜ |
| 10 | **密钥内存最小化** | `SQLCIPHER_KEY` / `DASHSCOPE_API_KEY` 仅在使用时读取，避免长驻与日志泄露；错误信息脱敏。 | 🟡 | ⬜ |
| 11 | **提交邮箱脱敏** | 当前 git 提交邮箱为 QQ 邮箱且已公开。可切换为 GitHub noreply 邮箱。 | 🟢 | ⬜ |

---

## 三、TTS 能力增强

| # | 方向 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 12 | **流式合成** | 后端已加 `POST /api/generate/stream`（本地边合成边推 PCM、云端合成后透传），返回 `audio/wav` 流。前端渐进播放（MSE/分块）待后续接入。 | 🔴 | 🚧 |
| 13 | **长文本分段** | 超长文本自动按句/标点切分、逐段合成再拼接（复用 `audio.concat_wavs`），避免单次请求超限或音质劣化。 | 🟡 | ⬜ |
| 14 | **云端在线复刻 UI** | 后端 `dashscope_tts.create_voice` 已就绪，但缺 OSS 上传闭环（云端复刻只接受公网 URL）。补 OSS 上传 + 前端「上传样本→创建音色→轮询 OK→可用」向导。 | 🟡 | ⬜ |
| 15 | **多 Provider 扩展** | `TTSProvider` 抽象已就位，可低成本接入更多引擎：火山/MiniMax 等云端，或 GPT-SoVITS/Fish-Speech 等本地。做成 provider 注册表 + 配置即插拔。 | 🟢 | ⬜ |
| 16 | **SFT/Instruct 正式支持** | 目前仅在前端灰显标注「不适用克隆」。可为其接入各自推理接口（预置音色 / 指令控制），补齐非克隆 TTS 模式。 | 🟢 | ⬜ |
| 17 | **样本预处理** | 克隆前对参考样本做静音裁剪、响度归一化、降噪（扩展 `audio.py`），提升音色稳定度。 | 🟡 | ⬜ |
| 18 | **模型预热** | 启动时可选后台预加载默认模型，消除工作室首次合成 ~1 分钟等待。 | 🟢 | ⬜ |

---

## 四、Companion Agent（项目愿景核心）

> 当前 `gen_agent.py` 只产出 `SystemPrompt.txt` + 知识库，尚未接入对话与语音闭环。这是从"能克隆声音"走向"能陪伴"的关键。

| # | 方向 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 19 | **对话闭环** | 已接入 LLM（百炼 Qwen）：新「陪伴」页 + `POST /api/chat`，用生成的 SystemPrompt 做角色扮演对话，回复再经 `/api/generate` 转克隆人声。知识库 RAG 见 #20。 | 🔴 | ✅ |
| 20 | **知识库 RAG** | 对知识库做向量检索，让 agent 回答贴合真实聊天风格与事实。 | 🟡 | ⬜ |
| 21 | **实时语音对话** | STT → LLM → TTS 全链路，实现近实时语音交流。 | 🟢 | ⬜ |
| 22 | **风格/情感分析** | 从聊天记录抽取用语习惯、情感倾向、高频话题，自动丰富 agent 人设 prompt。 | 🟢 | ⬜ |

---

## 五、体验与数据管理

| # | 方向 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 23 | **合成历史与版本** | 结果可保存历史、命名、A/B 对比不同音色/参数。 | 🟡 | ⬜ |
| 24 | **音色库管理页** | 统一管理本地样本与云端音色（预览、标签、删除、启用）。 | 🟡 | ⬜ |
| 25 | **多用户对照** | 管理台支持多账号数据概览与切换体验优化。 | 🟢 | ⬜ |
| 26 | **国际化/文档** | 补英文 README、架构图、贡献指南，降低他人使用门槛。 | 🟢 | ⬜ |

---

## 阶段规划建议

- **近期（打基础）**：#1 测试 · #2 CI · #8 Web 鉴权 · #12 流式合成 · #19 对话闭环
- **中期（补能力）**：#3 拆分 app · #4 日志 · #13 长文本 · #14 在线复刻 · #17 样本预处理 · #20 RAG · #23/#24 数据管理
- **远期（提格局）**：#7 瘦身仓库 · #15 多 Provider · #16 SFT/Instruct · #21 实时语音 · #22 风格分析 · #26 i18n

---

## 非目标（明确不做）

- ❌ 任何商业化 / 大规模分发用途；本项目仅限个人研究、需获对方授权。
- ❌ 将隐私数据（`private/`、`.env`、聊天记录、语音样本）纳入版本控制或对外发布。
- ❌ 绕过平台安全机制的功能扩展。

> 本 Roadmap 为动态文档，随进展更新状态标记。欢迎按优先级逐项推进。
